# R10 解答: 予約ワークフローの調査

## 現象と原因
Issueで観測された症状は単一層だけで断定できません。対応する境界または状態管理の契約が破れています。

## 調査順序
BrowserのHTTP、Gateway、FastAPI、必要に応じてRedis、PostgreSQL、WireMock、manifestを順に確認し、各観測で前段の仮説を除外します。

## 修正・回帰テスト・再発防止
根本原因に対応する最小修正を行い、同じ再現経路をテストで固定します。設定変更、API変更、無関係なリファクタリングは混ぜません。
