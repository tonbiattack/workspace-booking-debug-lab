# 設計方針

本教材では、予約という時間帯と状態遷移を持つ業務を使い、空き枠キャッシュ、タイムゾーン、二重予約、承認の原子性、外部カレンダー連携、Kubernetes設定差異を扱います。

FrontendはReact Queryで予約条件をキャッシュし、Go Gatewayは画面とFastAPIのHTTP境界を担います。FastAPIは予約契約、競合検知、キャッシュ保存条件を担います。各層で`X-Request-Id`を引き継ぎ、Browserから外部連携まで一つの操作を追跡可能にします。
